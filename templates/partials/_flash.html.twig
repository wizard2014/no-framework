<div class="row justify-content-center">
    <div class="col-6">
        {% if flash.has('error') %}
            <div class="alert alert-danger" role="alert">
                {{ flash.get('error') }}
            </div>
        {% endif %}
    </div>
</div>
